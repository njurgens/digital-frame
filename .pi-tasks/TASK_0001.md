---
id: TASK_0001
state: in_progress
phase: research
created_at: 2026-08-12T05:10:26.553Z
updated_at: 2026-08-12T05:25:23.206Z
title: Run `eng/check.sh` from the project root, identify all ruff docstring (D-series) violations reported by `uv run ruff check .`, and fix every violation in-place across the codebase so that `uv run ruff check .` passes with zero D-series errors.
label: Fix ruff D-series docstring violations
---

## raw prompt

Run the ruff checks with @eng/check.sh and fix all the docstring violations

## refined prompt

GOAL
  Run `eng/check.sh` from the project root, identify all ruff docstring (D-series) violations reported by `uv run ruff check .`, and fix every violation in-place across the codebase so that `uv run ruff check .` passes with zero D-series errors.

CONSTRAINTS
  - Preserve all existing code logic, function signatures, class definitions, and module structure; only modify docstrings.
  - Keep the existing `eng/check.sh` script unchanged; it is the authoritative check harness.
  - Do not alter `pyproject.toml` ruff configuration (`select = ["E", "F", "W", "I", "UP", "RUF", "ANN201", "ANN001", "D"]`) or any other project metadata.
  - Do not introduce new dependencies or remove existing ones.
  - Do not change formatting or trigger `ruff format` violations; docstring edits must remain compatible with the project's formatter.
  - Do not touch type-checking (`basedpyright`) concerns unless a docstring fix directly resolves a D-rule error.
  - Preserve all concrete identifiers verbatim (paths, function names, env vars, socket paths, config keys).

KNOWN-UNKNOWNS
  - Which specific D-rules are currently failing and in which files (requires running `uv run ruff check .` to surface the exact violations).
  - Whether the project follows a specific docstring convention (e.g., Google, NumPy, Sphinx/reST) beyond what ruff's default D rules enforce.
  - Whether non-D ruff violations (E, F, W, I, UP, RUF, ANN201, ANN001) are in scope or should be ignored for this task.

EXTERNAL-DEPENDENCIES
  - OneDrive  Microsoft OneDrive sharing API (used by framesync for photo sync via share URL)
  - mpv  mpv media player IPC protocol (JSON-over-Unix-socket communication at /tmp/mpv-socket)

